name: hs_workflow_intro_to_github_actions
on: [push]
jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Print "hello, world" with echo
        run: echo "hello, world"
